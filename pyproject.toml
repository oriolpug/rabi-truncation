[project]
name = "rabi-truncation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "scipy>=1.10",
    "matplotlib>=3.7",
    "pillow>=10.0",
    "qutip>=5.0",
    "tqdm>=4.0",
    "pytest>=8.0",
]
